include_directories(../RegLC)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

SET(LOCAL_SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/node_filter_utils.cpp
    )
    
SET(SWIG_SOURCES 
    ${SWIG_SOURCES}
    ${LOCAL_SOURCES}
    PARENT_SCOPE
    )
   
SET(MAIN_EXE_SOURCES 
    ${MAIN_EXE_SOURCES}
    ${LOCAL_SOURCES}
    PARENT_SCOPE
    )

SET(ALGO_INC_DIRS
    ${ALGO_INC_DIRS}
    ${CMAKE_CURRENT_SOURCE_DIR}
    PARENT_SCOPE
    )